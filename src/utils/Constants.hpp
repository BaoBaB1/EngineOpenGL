#pragma once

#include <limits>

namespace fury
{
  namespace constants
  {
    constexpr float fmin = std::numeric_limits<float>::min();
    constexpr float fmax = std::numeric_limits<float>::max();
  }
}
